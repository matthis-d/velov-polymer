language: node_js

node_js:
  - "0.10"

install:
  - npm install -g bower

script:
 - bower install

after_success :
  - rm .gitignore
  - git add .
  - git commit
  - git push -f git@heroku.com:velov.git master